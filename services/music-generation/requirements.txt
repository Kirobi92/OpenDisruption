fastapi>=0.111.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
aiofiles>=23.2.1
pydantic>=2.7.0
numpy<2
transformers==5.3.0  # pinned for reproducible builds + Dependabot-Tracking (OPE-166); audiocraft 1.3.0 + torch 2.1 constrain us to 4.x
audiocraft==1.3.0
# huggingface_hub: must stay <1.0 because transformers 4.x requires <1.0
# (transformers 5.x requires >=1.5.0 but is incompatible with audiocraft 1.3.0 + torch 2.1)
# Dependabot OPE-168: pin updated to latest 0.x (0.36.2) — upgrading to >=1.0 requires
# audiocraft upgrade + torch 2.4+ migration (tracked in OPE-163..OPE-167)
huggingface_hub>=0.36.2,<1.0
scipy
soundfile
xformers==0.0.22.post7